// import { createSelector } from "@ngrx/store";
// import { IssuesStateInterface } from "../../models/issues-state.interface";
// import { DataStateInterface } from "../../models/issues.interface";

// export const selectFeature = (state: {issues: DataStateInterface}) => state.issues

// export const selectLoadingIssues = createSelector(
//     selectFeature,
//     (state) => state.isLoading
// );